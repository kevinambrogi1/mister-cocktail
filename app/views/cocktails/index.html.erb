
<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2"><h1>List of cocktails</h1>

      <% @cocktails.each do |cocktail| %>
      <%= cocktail.id %>. <%= link_to cocktail.name, cocktail_path(cocktail) %>
      <div> Number of ingredients: <%= cocktail.ingredients.size %></div>
      <br>
      <% end  %>
      <%= link_to "Create new", new_cocktail_path, {class: "btn btn-primary"} %>
    </div>
    <div>blah</div>
  </div>
</div>


